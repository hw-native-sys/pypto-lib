name: Docs

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: docs-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

# TEMPORARY (draft experiment, revert before merge): every job below is
# gated off on branch exp/ptoas-op-fusion-ab. That branch exists only to
# collect a ptoas op-fusion A/B from the A5 daily sweep, and nothing it
# changes is exercised by these jobs.
jobs:
  build:
    if: github.ref != 'refs/heads/exp/ptoas-op-fusion-ab' && github.head_ref != 'exp/ptoas-op-fusion-ab'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip
          cache-dependency-path: docs/requirements.txt

      - name: Install documentation dependencies
        run: python -m pip install -r docs/requirements.txt

      - name: Validate documentation structure
        run: |
          python tests/lint/check_docs_nav.py
          python tests/lint/check_public_docs.py
          python -m unittest discover -s tests/docs -v

      - name: Build documentation
        env:
          DOCS_REF: ${{ github.event_name == 'push' && 'main' || github.sha }}
        run: mkdocs build --strict

      - name: Upload Pages artifact
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v4
        with:
          path: site

  deploy:
    if: github.ref != 'refs/heads/exp/ptoas-op-fusion-ab' && github.head_ref != 'exp/ptoas-op-fusion-ab'
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deploy
        uses: actions/deploy-pages@v4
