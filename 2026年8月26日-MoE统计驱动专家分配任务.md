# 2026年8月26日 MoE 统计驱动专家分配任务

## 1. 要解决的问题

原来的流程：

- 当前维护的 EPLB 基线分支为 `upstream/perf/dsv4-eplb-decode-logits-and-mtp-core`，原始基线提交为 `4c633d4`。新平行分支直接以 `upstream/main@85e0790` 为唯一基线，把 EPLB overlay、stats placement 和任务记录合并为一个 feature commit；原 EPLB 分支和入口未修改。正式设备验证点为整理前的 `16306ee`，后续 rebase 经 `git range-diff` 确认为 patch-identical，squash 前后 feature tree 保持一致。
- 该分支使用 EP8、每 rank 16 个专家、共 128 个物理专家的合成负载。
- 384 条 route 被均匀分配到 128 个专家，每个专家固定收到 3 条 route；各层复用同一份 round-robin `tid2eid`，MoE 调用被固定到 hash layer 0。
- dispatch 直接把 expert ID 当作物理 ID，按 `dst_rank = eid // N_LOCAL`、`local_slot = eid % N_LOCAL` 定位专家，没有独立的逻辑专家到物理 slot 映射。
- 该流程适合做理想均衡负载下的性能基线，但不能表达 `moe-stats.jsonl` 中逐层不同的专家热度。

具体问题：

- `moe-stats.jsonl` 对应 EP8、每 rank 32 个专家、共 256 个专家，与现有 EPLB 分支的 128 专家拓扑不一致，不能只替换 `tid2eid`。
- 文件包含 215 次 dispatch、44 层，其中主模型为 layer 0～42，MTP 为 layer 43；每层提供 256 个 `expert_token_counts`。
- phase 包括 4 条 `prefill`、64 条 `mtp_prefill` 和 147 条 `decode_mtp`。其中 141 条完整 `decode_mtp` 的每层 route 数为 384，与当前 decode benchmark 的 route 规模一致。
- stats 只提供每次 dispatch、每层、每个专家的 route 计数，没有逐 token 路由、source-rank 到 expert 的流量矩阵、请求 ID、当前物理映射和专家执行时间。因此可以优化目标 rank 的专家负载，但不能精确重放原始 AllToAll 通信，也不能直接推导端到端加速比。
- “不再每层 EPLB”的准确含义待确定。本文默认解释为：不在运行时逐层执行 EPLB，而是离线生成逐层固定 placement，部署期间直接使用静态结果。逐层静态表不等于运行时逐层 EPLB。
- 如果需求是 44 层共用同一张 placement，是否必须如此待确定。当前数据初步显示各层热点差异很大，共用一张表难以平衡单层负载，因此不建议作为默认实现。

本次目标：

- 已新建平行分支 `perf/dsv4-stats-placement-decode-logits-and-mtp-core`，保留现有 EPLB 分支和入口作为基线。
- 第一阶段固定为 EP8 × 32、256 个逻辑专家、每个专家一个物理副本；是否支持专家复制待确定。
- 根据 `moe-stats.jsonl` 离线生成 44 份固定 placement，运行时不再计算 EPLB。
- 当前 manifest 已绑定 stats SHA256、schema、phase 过滤条件、算法版本和拓扑。新增 fail-closed checkpoint/cache binding 协议：checkpoint 绑定 config/index 语义 hash、完整 safetensors inventory 与 shard SHA256；cache 绑定 source/converter/ABI/layout/placement fingerprint。原始 stats 不含 checkpoint 标识，因此候选 checkpoint 选择和真实 loader 接入仍待确定，不能据目录名猜测绑定。
- 在相同 256 专家、相同权重、相同输入和相同路由负载下，对比 contiguous placement 与 stats placement 的正确性和真实性能。
- 在同一 EP8×32 workload 中新增 EPLB 对照：用本次 replay 的 384 条逻辑 route 计数离线求解 256 专家单副本布局，三个 variant 只改变逻辑专家到物理 slot 的映射。
- 第一版覆盖 MoE EP8 单层、Decode-FWD EP8 和 Decode-MTP EP8 三个 stats-shaped hash-route replay benchmark；prefill、真实 checkpoint 权重加载和生产部署仍待确定。

分支创建：已完成，并使用独立 worktree 隔离开发。

代码实现：已完成第一版方案 A。新增离线生成器、版本化 manifest、host 侧 9 类 MoE tensor 同步置换、layer 0 单层 MoE 入口、43 层 full decode 入口、layer 43 MTP 入口，以及相同 EP8×32 workload 下的 contiguous/stats/EPLB 三方 runner；stats-placement 的 MoE、Decode-FWD 和 Decode-MTP 三个入口均已接入 numeric Golden，dispatch kernel ABI 和运行时关键路径未增加 mapping lookup。

PR：待确定。

## 2. 数据流

输入：

- 文件：`moe-stats.jsonl`。
- 记录数：215。
- 拓扑：`ranks=8`、`local_experts=32`、总专家数 256。
- shape：每条记录包含 44 层，每层 `expert_token_counts` 的 shape 为 `[256]`。
- layout：`expert_token_counts[i]` 暂按重映射前的逻辑 expert `i` 理解；它是否已经经过某种物理重映射仍待确定，进入真实 checkpoint 或生产使用前必须由数据生产方确认。
- 所在 rank：stats 是 host 侧离线输入，不属于任何 device rank。
- 优化 phase：第一版固定使用 `phase=decode_mtp` 且 `routed_tokens=384` 的 141 条完整 dispatch，避免将 prefill、MTP prefill 和 decode 按采样条数直接混合。
- 当前 trace 约 54 秒；采集工具、模型/checkpoint、流量来源、采样规则、phase 权重、dispatch 时间范围和长期代表性均待确定。

处理过程：

1. 逐行解析 JSONL，校验 schema、dispatch 顺序、44 层、每层 256 个计数，以及 `sum(expert_token_counts) == routed_tokens`。
2. 按确定后的 phase、dispatch 范围和权重聚合每层每个逻辑专家的 route 数。
3. 对每层独立求解 256 个逻辑专家到 8 × 32 个物理 slot 的双射，约束每个 rank 恰好放置 32 个专家，目标是降低最大 rank 的预测负载。
4. 第一版已实现带确定性 tie-break 的 capacity-constrained LPT；未实现 local swap、ILP、p95-aware 或多 phase 最坏值目标，是否需要这些扩展待确定。
5. EPLB 对照另行按 `deepseek-ai/EPLB@d52c72d5` 的无冗余 balanced packing 语义求解，保留上游 FP32 `torch.sort(descending=True)` 的专家顺序和 tie 行为。
6. 生成版本化 manifest，记录 source SHA256、schema、算法版本、phase 过滤条件、拓扑、逐层映射和预测负载。
7. 按方案 A 在 host 侧展开为物理布局，或按方案 B 在 dispatch 前执行逻辑 ID 到物理 ID 的查表。
8. stats-shaped hash fixture 已采用确定性的直方图重建规则生成合法 `[tokens, TOPK]` 路由，只保证逐专家计数一致。stats 不包含 token 级共现和 source-rank 信息，因此该 fixture 不是原始 token、route 顺序或通信流量的精确 replay。
9. 如果使用真实 gate 路由，则需要加载真实输入和权重；数据来源及 golden 方式待确定。

输出：

- 主映射 shape：`logical_to_physical[44][256]`。
- 建议同时输出的逆映射 shape：`rank_to_logical[44][8][32]`。
- layout：`physical_eid = rank * 32 + local_slot`，每层映射必须是 `0..255` 的双射。
- 所在 rank：manifest 在 host 侧生成和校验；方案 A 在加载权重时展开，方案 B 还需要把映射复制到各 device rank。
- provenance：当前已记录 stats SHA256、phase/dispatch 过滤条件、算法及版本和拓扑；checkpoint/cache 的 fail-closed fingerprint 协议已实现，但尚未选择有采集端 provenance 的真实 checkpoint，也未接入真实 loader。
- manifest 使用版本化 JSON，存放于 `models/deepseek_v4_flash_mtp/stats_placement_decode_manifest.json`；运行时只加载 manifest，不直接解析约 5.8 MB 的原始 JSONL。原始 `moe-stats.jsonl` 当前未提交，文档也未记录可审计归档位置，clean clone 不能独立重建 manifest；其提交、归档或可审计重建方式待确定。

## 3. 方案选择

候选方案 A：离线逐层固定 placement，并在 host 侧重排 router 数据和专家权重。

处理方式：

- hash-routing 层把 `tid2eid` 中的逻辑 expert ID 转成物理 expert ID。
- score-routing 层按同一 placement 重排 `gate_w` 和 `gate_bias`，使 top-k 直接输出物理 expert ID。
- routed expert weights 和 scales 按逐层 placement 放入对应 rank/local slot。
- dispatch 保持现有 `eid // N_LOCAL` 和 `eid % N_LOCAL` 逻辑，不增加运行时查表。
- placement 是部署前生成的 44 份静态表，不在每层执行时重新计算。

优点：

- kernel ABI 和 dispatch 关键路径基本不变，运行时没有额外 mapping lookup。
- 现有 dispatch、通信窗口和大部分 golden 逻辑可以复用，第一阶段改动范围较小。
- manifest、权重和路由表可以通过 fingerprint 强绑定，结果容易复现。
- 最适合先验证 stats placement 是否能在真实 EP8 设备上带来收益。

缺点：

- placement 变化后需要重新转换或加载 gate 和 routed expert 权重。
- 静态布局不能自动适应 workload 漂移，也不能在 prefill/decode 之间无成本切换。
- 完全相同 score 的专家可能因 gate 行重排改变稳定排序的 tie 顺序；是否接受待确定。
- 若先构造完整 stacked weights 再重排，可能增加接近一份权重的 host 内存，应尽量在 loader 中直接 gather 到目标布局。

候选方案 B：router 保持逻辑 expert ID，在运行时查表得到物理 expert ID。

处理方式：

- hash `tid2eid`、`gate_w` 和 `gate_bias` 保持逻辑专家顺序。
- 新增 `logical_to_physical[layer][logical_eid]` mapping tensor，在 dispatch 前把逻辑 ID 转成物理 rank/local slot。
- routed expert weights 仍必须按所选 placement 放到物理 slot，mapping 与权重布局必须使用同一版本。
- 第一阶段只考虑模型加载时选择一份静态 mapping。运行中周期性重新分配还需要专家权重迁移、双缓冲、全 rank 同步和回滚，是否需要另行待确定。

优点：

- router 的逻辑专家顺序不变，能够保留原有 top-k tie 语义。
- placement 与 router 参数解耦，同一模型可以在加载时选择不同 profile。
- 接口更接近后续 phase-specific placement、专家复制或在线调整的演进方向。

缺点：

- 需要修改 `moe`、dispatch、golden、tensor specs 和各模型入口，kernel ABI 影响更广。
- 每条 route 增加查表和依赖，可能侵蚀负载均衡收益，具体开销待实测。
- 所有 rank 的 mapping 与权重版本必须一致，否则会把 token 发给错误专家并产生静默错误。
- 仅更新 mapping 不能移动专家，真正在线调整还需要权重迁移、同步、安全切换点和额外内存，第一阶段不具备这些能力。

我的选择：

我倾向于先实现方案 A，因为当前需求首先需要验证“历史 stats 生成的固定 placement”是否有效，而方案 A 对 kernel 关键路径和现有 EPLB overlay 的侵入最小。两种方案可以共用同一种 manifest schema；只有在实测证明 workload 漂移或 phase 冲突足以覆盖查表和迁移成本后，再考虑方案 B。

## 4. 关键实现

| 函数/模块 | 输入输出 | 职责 | 为什么需要 |
|---|---|---|---|
| `tools/generate_dsv4_moe_placement.py` | JSONL `[dispatch][44][256]` → manifest `[44][256]` | 严格校验 stats、过滤 phase、聚合负载、生成确定性 placement 和 provenance | 把运行时 EPLB 变成可复现的离线步骤 |
| `models/deepseek_v4_flash_mtp/eplb_placement.py` | 本次 replay `[256]` route 计数 → EP8×32 单副本布局 | 实现与上游 DeepSeek EPLB 一致的无冗余 balanced packing | 为 stats placement 提供同拓扑、同 workload 的 placement oracle |
| `models/deepseek_v4_flash_mtp/stats_placement_fixture.py` | manifest → 双射、容量、版本和 hash 校验结果；logical route/weights → physical route/weights | 校验 manifest、生成 stats-shaped fixture，并集中实现 host 侧 permutation | 防止错误布局或旧缓存被静默使用，并保证 9 类 MoE tensor 使用同一映射 |
| `models/deepseek_v4_flash_mtp/checkpoint_contract.py` | checkpoint/cache → versioned fingerprint manifests | 校验 checkpoint JSON、safetensors inventory 与所有 shard bytes；绑定 converter、ABI、layout 和 placement | 使真实 loader 在有明确 source provenance 时可以 fail-closed 地拒绝错 checkpoint 或错 cache |
| `models/deepseek_v4_flash_mtp/device_output_ab.py` | post-Golden selected rows → immutable artifact / direct A/B result | 以 placement-independent logical rows 捕获低容量 device output，并逐行比较 | 补充“各自过 Golden”之外的 contiguous-vs-stats 直接设备输出对照 |
| `models/deepseek_v4_flash_mtp/stats_placement_moe.py` | layer 0 placement + 8 token/rank → `x_next` | 用完整 384 条 layer 0 路由测试独立 MoE EP8，并执行 numeric golden | 补齐正式三场景中的 MoE EP8；不修改旧 EP8×16 入口 |
| `models/deepseek_v4_flash_mtp/stats_placement_decode_logits.py` | layer 0～42 placement + decode inputs → 11 个 decode 输出 | 复用原 EPLB kernel，逐层切换静态布局并执行 full numeric Golden | 新入口与原 EPLB 入口隔离，避免改变基线行为 |
| `models/deepseek_v4_flash_mtp/eplb_decode_logits_golden.py` | 43 层 decode 输入 → cache/state/hidden/logits | 串联 SWA、CSA、HCA、MoE 和 LM head 的完整 Torch Golden，并为 11 个输出提供 comparator | 让 stats Decode-FWD 从只跑通升级为数值验证 |
| `models/deepseek_v4_flash_mtp/stats_placement_mtp_core.py` | layer 43 placement + MTP inputs → MTP outputs | 为 MTP 使用独立 placement | MTP 热点分布和主模型不同，不能复用 layer 0 |
| `models/deepseek_v4_flash_mtp/gate.py` | 逻辑 router 数据 → 物理 ID（A）或逻辑 ID（B） | 方案 A 使用重排后的 gate/hash 数据；方案 B 保持逻辑 ID | 保证 gate 选择的专家与实际物理权重一致 |
| `models/deepseek_v4_flash_mtp/moe.py` | routed IDs → rank/local slot | 方案 A 保持不变；方案 B 增加 logical-to-physical lookup，并同步 golden | dispatch 当前直接把 expert ID 当物理 ID |
| `models/deepseek_v4_pro/utils/weights_flash.py`（若接真实权重） | checkpoint + placement → 各 rank 物理专家权重 | 在权重加载时直接按 placement gather，并绑定转换缓存 fingerprint | 避免逻辑 ID、物理 slot 和转换缓存不一致 |
| `tests/contract/test_dsv4_*stats*` | identity/permutation/异常 manifest → pass/fail | 校验映射、host transform、route 合成、接口、overlay 和 numeric Golden contract | 防止错误映射、降级验证模式或缺少 comparator 被静默接受 |
| `tools/run_dsv4_stats_placement_perf.sh` | contiguous/stats/EPLB runs → fastest/max-rank latency与 rank spread | 在相同 EP8×32 workload 下自动对比，并将 MTP cleanup 与 compute 分离 | 旧 EP8×16 EPLB 不是有效性能基线 |

## 5. 同步与生命周期

- 谁生产数据：`moe-stats.jsonl` 由外部采集流程生产，采集工具、模型版本、checkpoint、流量来源和采样规则待确定；placement manifest 由离线生成器生产。
- 谁消费数据：host 侧 fixture 或权重 loader 消费 manifest；方案 A 的 kernel 只消费已经重排的物理 ID/权重，方案 B 的 kernel 还消费 mapping tensor。
- 如何通知 ready：方案 A 当前只在 host 侧校验 manifest schema/hash、拓扑和 mapping 一致性，不增加 device 侧 ready 通知；真实 checkpoint/cache binding 的协议已经可用，但真实 loader 的调用点与输入 provenance 仍待确定。方案 B 还需要保证 mapping 在 dispatch 前已复制到所有 rank，具体 barrier/event 机制待确定。
- buffer 什么时候可以复用：原始 stats 在生成 manifest 后不进入运行时；manifest 和重排后的权重在整个模型实例生命周期内保持有效。方案 B 的 mapping buffer 在所有引用该版本的 in-flight dispatch 完成前不可复用或覆盖。
- 出错可能导致什么：路由 ID、gate 行、专家权重、scales 或 mapping 任一侧版本不一致，会把 token 送到错误专家，可能产生数值正常但语义错误的静默结果；不同 rank 使用不同 mapping 还可能导致错误通信和越界。
- 多版本切换：第一阶段不做运行中 mapping 切换。若后续需要，权重迁移、双缓冲、safe point、全 rank 原子发布和回滚协议全部待确定，并应单独立项。

## 6. 正确性与性能验证

正确性：

- 测试方式：
  - 校验 215 条 stats 均可解析，每条 44 层，每层 256 个计数，且计数和等于 `routed_tokens`。
  - 校验每层 placement 是 `0..255` 的双射，每个 rank 恰有 32 个专家，相同输入重复生成结果一致。
  - 分别验证 `tid2eid`、`gate_w/gate_bias`、routed weights/scales 的 permutation 使用同一映射。
  - 做 metamorphic 测试：contiguous logical layout 与重排后的 physical layout 在相同逻辑输入下输出等价。
  - 覆盖 hash layer、score layer、主模型 layer 42、MTP layer 43、identity mapping 和随机 permutation。
  - stats-placement 的 MoE、Decode-FWD 和 Decode-MTP 三个正式入口均使用 numeric Golden：MoE 校验 `x_next`；Decode 执行 43 层完整 Torch Golden 并校验 11 个输出；MTP 校验 4 个数值输出。
  - legacy 的默认验证口径保持不变：MoE 为 numeric Golden，Decode 为 runtime-only，MTP 为 finite-only；共享 Golden plumbing、默认不生效的 CLI 选项和 compressed metadata 边界检查也有改动，不能表述为 legacy 代码完全未变。
  - synthetic 路径中的 manifest/schema/hash、拓扑和 mapping 不一致会显式失败；checkpoint/cache binding manifest 的 schema、JSON 语义 hash、shard inventory、source/converter/ABI/layout/placement mismatch 均显式失败。真实 checkpoint provenance 与 loader 接入仍待确定。
- 精度口径：MoE `x_next` 固定校验 64 个 rank-token 行，同时保留全局 `diff=0.003/pct=5%`，并以逐行 `diff=0.003/pct=10%` 防止单行错误被全局比例稀释。MTP 的 KV 固定 64 个 mapped physical rows，每行 `0.01/5%`，所有 unmapped rows 精确不变；`hidden_out`、`next_pre_hc_hidden`、`logits` 各固定 64 行，逐行阈值分别为 `0.02/10%`、`0.02/5%`、`0.02/10%`，logits 活跃性由 `logit_row_indices` 决定。所有 mapping 仅允许 `-1` 作为负哨兵。完整 Decode 使用 active-row semantic gate：cache/state 使用 `cosine=0.99`、`relative-L2=0.10` soft threshold、2% soft budget和 hard envelope，unmapped physical rows 精确不变，hidden/logits 逐 rank-token 行检查；纯 permutation 的 host 结果精确一致。
- 边界场景：零热度专家、单专家热点、相同负载 tie、相同 gate score tie、tail dispatch（96/192/288 routes）、错误拓扑、缺层、重复 expert、过期 manifest、MTP 独立布局。
- 本地验证：424 项 Golden、runner、placement 与 overlay 回归全部通过；Ruff、header、English-only 和 `git diff --check` 已通过。CANN 仅报告已有的安装目录 owner warning。
- 正式设备环境：`conda wq3`、PyPTO `8ccd97de3ffd`、CANN 9.0.0、PTOAS `/usr/local` 0.60、偶数卡 `0,2,4,6,8,10,12,14`、seed 1807、warmup 5、measured 100，均为 live Golden（`golden_replayed=false`）。
- clean-source 设备输出直接 A/B（补充正确性证据，不属于正式性能测试）：任务 `task_20260831_084336_32217866531` 使用 `conda wq3`、`a2a3`、奇数卡 `1,3,5,7,9,11,13,15` 和 seed 1807，依次验证 MoE EP8、Decode-FWD EP8×TP4 和 Decode-MTP EP8×TP4。每个 case 的 contiguous reference 与 stats candidate 绑定相同的 manifest、拓扑、seed 和 `program_source_sha256`；两侧均通过 Numeric Golden，stats candidate 再通过 v2 artifact 的 direct device-output A/B，三组均为 `PASS`。该轮只用于正确性，奇数卡耗时不进入正式性能表，也不与偶数卡结果比较；正式性能测试仍固定使用偶数卡 `0,2,4,6,8,10,12,14`。
- Decode-FWD EP8 TP4：任务 `task_20260830_212723_340869924456`、artifact `build_output/dsv4_stats_numeric_all_even_v3_final_ptoas060_20260830/`，11 个输出全部 `PASS`。coverage 为 KV/HCA/CSA/CSA-inner `2752/1280/1344/1344`，compressed KV 与 idx cache/scale 当前为 0，pre-hidden/hidden/logits 均为 64；最快 device 14 的 min/median/mean/max 为 `39906.600/40020.950/40279.270/50726.000 us`，最大 rank median 为 `62486.600 us`。该 artifact 元数据是 v3；Decode comparator 语义未变，已由当前 v4 parser 重验通过，不能把原 artifact 描述为 all-v4。
- Decode 最差数值指标：KV 为 11/2752 bad rows、min cosine `.984916`、max relative-L2 `.173714`；HCA 为 14/1280、`.980998/.314251`；CSA 为 7/1344、`.985940/.220578`；CSA-inner 为 8/1344、`.982992/.223761`；pre-hidden/hidden/logits 的 min cosine 为 `.996280/.996401/.996429`，max relative-L2 为 `.086307/.084834/.084485`。
- Decode 严格逐点首轮曾失败，后续测量显示误差按层平滑积累且未发现 schedule/mapping 错误；该历史 probe 只用于制定并冻结上述 semantic gate，不作为最终 PASS 证据。
- Decode-MTP EP8 TP4：任务 `task_20260830_221343_14178577904`、artifact `build_output/dsv4_stats_numeric_mtp_even_v4_rowwise_probe_20260830/`，v4 的 KV、hidden、next、logits 均为 `active_rows=64` 且逐行 `PASS`，parser stderr 为空。最快 device 14 compute 的 min/median/mean/max 为 `1274.800/1295.450/1347.649/3244.800 us`，最大 rank compute median 为 `17459.200 us`，cleanup median 为 `12.900 us`。
- MoE EP8：逐行 5% probe `task_20260830_221107_10787321577` 正确失败，暴露 5/64 行超过门限、最差 8.8196%；据此保留原全局 5% 并增加逐行 10% hard budget，升为子合同 `dsv4-stats-placement-moe-ep8-v3`。最终任务 `task_20260830_221942_176849525131`、artifact `build_output/dsv4_stats_numeric_moe_even_v3_row_and_aggregate_final_20260830/`，`x_next active_rows=64`、组合门限 `PASS`、parser stderr 为空；最快 device 14 的 min/median/mean/max 为 `863.400/869.300/914.993/3447.600 us`，最大 rank median 为 `5514.150 us`。
- Decode compressed mapping 边界：正式口径 `start_pos=8192` 时，三类 compressed metric 的 `active_rows=0`，`idx_kv_cache` 也没有 active write。补充真机任务 `task_20260831_022303_415394926054` 在 `start_pos=8191` 覆盖 HCA/CSA compressed 写入 `640/672` 行，连同 state、idx cache/scale、hidden/logits 共 11 个输出全部 PASS。idx cache 的逐点 INT8 误差比例为 `44.0883%`，因此以映射后 logical row 的 semantic gate 判定；672 行中 23 行超过 soft 门限（3.4226%），仍在 3.5% budget 和 hard envelope 内。
- Golden 数据边界：完整 Decode 输入约 `325 GiB`，当前使用只读共享的低内存 live Golden；已提供 `--save-data`/`--golden-data`，但磁盘约仅 `253 GiB` 空闲，未生成 replay snapshot，当前 `golden_replayed=false`。
- 数值结论边界：MoE、Decode-FWD、Decode-MTP 三个 stats 入口已有偶数卡 Numeric Golden 证据；Decode-FWD 的 `start_pos=8191` compressed-cache 活跃写入也已有真机 PASS。此外，clean-source 上三个 case 已分别完成 contiguous reference 与 stats candidate 的 direct device-output A/B，并按各自 comparator 逐 logical row 通过。该补充证据只覆盖 synthetic stats-shaped workload 下的 placement 等价性，不验证真实 checkpoint/gate 的专家 ID 语义，也不表示该分支已 production-ready。parser 会精确检查 reference、comparator、active rows 和最终 PASS，但依赖可信 runner/log，只防普通验证降级，不是防篡改证明。

性能：

- 对比对象：contiguous、stats 和 EPLB 共用 EP8×32、256 专家、seed 1807、逻辑 route、权重和输入；只改物理 placement。旧 EP8×16 EPLB 基线未修改，也不参与本表对比。
- EPLB 对照是使用当前 replay 精确直方图的离线 placement oracle；求解、权重迁移和同步不计入时延，不代表完整在线 EPLB 成本。
- route-count proxy：44 层的最大 rank route 数均值为 contiguous `85.93`、stats `49.80`、EPLB `48.98`；全层最大值分别为 `138/61/61`。EPLB 在 32 层严格优于 stats、12 层持平，但这不是设备性能指标。
- 当前工作树性能任务：`task_20260827_210244_374464728761`，`conda wq3`，偶数卡 `0,2,4,6,8,10,12,14`，5 次 warmup、100 次 measured rounds，退出码 0，6 个 variant 全部 `pass`。
- 补充 MoE 性能任务：`task_20260829_014128_35133831025`，环境、卡组、seed 和采样口径相同，3 个 variant 全部 `pass`。artifact 为 `build_output/dsv4_stats_placement_perf/board_wq3_even_moe_ep8x32_20260829/`。
- provenance 边界：metadata 的 `git_sha=567f302` 是 worktree HEAD；Decode/MTP 任务的 `source-status.txt` 记录了 9 个未提交/未跟踪文件，补充 MoE 任务记录了 11 个，因此不能只靠该 SHA 重建测量代码。合并为单 commit 后的 clean-SHA 复测待确定。
- artifacts：`build_output/dsv4_stats_placement_perf/board_wq3_even_eplb_upstream_567f302/`，目录名中的 SHA 仅表示 HEAD；`comparison.tsv` 保留旧 stats-vs-contiguous v1 schema，`stats-vs-eplb.tsv` 记录新对照。生成物不提交。

| case | contiguous fastest (us) | stats fastest (us) | EPLB fastest (us) | stats vs contiguous | stats vs EPLB |
|---|---:|---:|---:|---:|---:|
| MoE EP8×32 · layer 0 | 829.000 | 843.700 | 844.500 | +1.773% | -0.095% |
| decode-logits | 39819.150 | 37877.300 | 40546.700 | -4.877% | -6.584% |
| mtp-core | 1247.550 | 1209.500 | 1211.300 | -3.050% | -0.149% |

- MoE 的 coordinated bottleneck（最大 rank median）为 contiguous `5098.550 us`、stats `5020.400 us`、EPLB `4955.000 us`；stats 相对 contiguous 改善 `1.533%`，相对 EPLB 慢 `1.320%`。layer 0 的最大 rank route 数分别为 `53/49/48`，设备结果方向一致但幅度较小。
- 结论：MoE layer 0 上 stats 的 bottleneck 优于 contiguous、弱于 EPLB oracle；decode 上 stats 明显快于 EPLB；MTP 两者接近。EPLB 的 route-count proxy 略优但 decode 实测更慢，说明单纯最大 rank route 数不足以预测性能；local-slot 顺序、调度或更细粒度通信影响待 trace 确定。
- 当前口径：comparison schema 为 v2；Decode/MTP numeric contract 为 `dsv4-stats-placement-numeric-v4`，MoE 子合同为 `dsv4-stats-placement-moe-ep8-v3`。runner 要求三个 stats 入口全部为 `numeric_golden`，并精确校验 Golden reference、comparator 和 active-row coverage；legacy profile 的默认口径仍为 MoE numeric、Decode runtime-only、MTP finite-only。上表历史三方 A/B 不回溯改写，本轮 stats-only Golden 重跑也不用于重新计算 contiguous/EPLB 差值。

## 7. 我的判断与疑问

我的判断：

- 原 stats-vs-contiguous A/B 已有三次、两个设备集合的同方向证据，可视为 synthetic stats-shaped benchmark-stable。新增 EPLB 三方对照目前只有一次 dirty-worktree 性能任务，clean-SHA 重复前不单独宣称 benchmark-stable；整个分支仍不是 real-checkpoint 或 production-ready。
- 需求可以实现，但应定义为“离线按层生成静态 placement”，而不是 44 层共用一张映射，也不是运行时每层重新做 EPLB。
- 第一版已采用方案 A、EP8×32、256 专家一一放置，不做复制，不改 dispatch 关键路径。
- 现有均匀 round-robin fixture 已替换为确定性的 stats-shaped workload；真实 gate workload 仍待确定。
- MTP layer 43 已使用独立 placement。
- 单层 MoE EP8 固定使用 layer 0 placement；8 个 rank 的 `input_ids` 连续覆盖 0～63，确保恰好消费该层完整 384 条逻辑 route。旧 EP8×16 `moe.py --balanced-routing` 基线未修改。
- 两种方案应共用同一 manifest 表达和 fingerprint 规则，避免后续重复设计。

目前的担忧：

- stats 只有约 54 秒数据，且疑似只覆盖少量请求，是否代表线上长期流量待确定。
- prefill、mtp_prefill 和 decode 的热点分布不同，错误的 phase 权重可能提升一种场景却损害另一种场景。
- stats 缺少 source-rank 流量，计算负载改善不一定等比例改善 AllToAll 或端到端时延。
- stats 缺少 token 级路由，合成 fixture 不能恢复原始 token 共现、route 顺序和源 rank 分布。
- 本次 EPLB 的 route-count 均衡 proxy 略优于 stats，但 decode 更慢；未做更细粒度 trace 前不对原因定性。
- router ID、物理专家权重和 manifest 只要有一侧错位，就可能出现难以察觉的静默正确性问题。
- full Decode 已有 synthetic Torch Golden，最终门禁采用 semantic gate；`start_pos=8191` 的 compressed cache 活跃写入已有单独真机证据，但真实 checkpoint/gate 语义仍未覆盖。

已确定：

- 第一版是 44 层各自一张离线静态 placement，不是运行时逐层 EPLB，也不是 44 层共用一张表。
- 第一版是 EP8×32、256 专家单副本双射；stats 使用确定性 capacity-constrained LPT，EPLB 对照使用上游无冗余 balanced packing。专家复制和在线迁移不在本版范围。
- 三个 variant 共用同一份 256 专家逻辑路由、权重和输入；EPLB 仅作为 placement-quality oracle，求解和权重迁移在计时区间外。
- 第一版使用 141 条完整 `decode_mtp` 统计生成确定性的 stats-shaped hash-route fixture，不声称精确 replay 原始 token 或通信。
- 生成器和版本化 manifest 放在平行分支 `perf/dsv4-stats-placement-decode-logits-and-mtp-core`。截至 2026-08-31，本地与远端分支均以 `upstream/main@0e7e07a` 为基线，并只包含一个 capability commit（`upstream/main...branch = 0 1`）；原 EPLB 分支未修改。后续日常 rebase main 后使用 `--force-with-lease` 更新远端。
- 正式本地与真机验证使用 `conda wq3`；正式性能口径为偶数卡 EP8、seed 1807、warmup 5、measured rounds 100。

待确定：

- 待确定：由数据生产方确认 `expert_token_counts[i]` 是否是重映射前的逻辑 expert `i`，并补齐采集工具、模型/checkpoint、流量和采样元数据。
- 待确定：原始 `moe-stats.jsonl` 的提交、归档或可审计重建方式；当前 clean clone 只能消费 checked-in manifest，不能独立重建它。
- 待确定：采集端提供真实 checkpoint/gate 输入 provenance 后，选择对应 checkpoint，生成并保存 fingerprint manifest，并把它接入真实 loader 的置换路径。
- 待确定：是否按明确权重同时优化 prefill、mtp_prefill 和 decode；prefill/decode 是共享常驻布局还是允许 phase 边界安全切换。
- 待确定：decode 上 EPLB route-count proxy 与设备性能方向不一致的确切原因，以及是否需要 chip swimlane/AllToAll 细分。
- 待确定：生产版在线 EPLB 的统计窗口、求解频率、权重迁移、同步和回滚协议；本次对照不覆盖这些成本。
- 待确定：是否增加跨天/跨机重复、置信区间和更严格的性能合入线；是否在后续版本支持热点专家复制。
- 待确定：在分支唯一 clean capability commit 上，使用偶数卡 `0,2,4,6,8,10,12,14` 复跑同口径 contiguous/stats/EPLB 正式性能；本次奇数卡 direct A/B 仅是正确性证据，不能替代该性能复测。
- 待确定：owner、排期和 PR；该分支以单 commit 远程维护，日常 rebase main 后使用 `--force-with-lease` 更新。

如果重新设计，我可能会：

- 让采集端增加 source-rank × logical-expert 的流量矩阵、模型/checkpoint fingerprint、TOPK、token 数和每个专家的实测执行成本。
- 将优化目标从单纯 route 数升级为预计 tile 工作量、专家 kernel cost、AllToAll 流量和跨 dispatch p95 的组合目标。
- 如果长期 workload 漂移明显，再引入多 profile 或方案 B；运行中在线迁移和专家复制另行设计，不放入第一版。
